definitions:
  pagination:
    limit:
      in: query
      name: limit
      description: limit count
      required: false
      schema:
        type: integer
      example: 100

    offset:
      in: query
      name: offset
      description: page index
      required: false
      schema:
        type: false
      example: 0

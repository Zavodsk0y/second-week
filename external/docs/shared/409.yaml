definitions:
  409:
    description: Conflict
    content:
      application/json:
        schema:
          type: object
          properties:
            message:
              type: string
              example: "Поле login должно быть уникальным."
          required:
            - message
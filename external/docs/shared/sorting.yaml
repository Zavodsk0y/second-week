definitions:
  sortCreatedAt:
    name: sortCreatedAt
    in: query
    description: Sort todos with createdAt field
    required: false
    schema:
      type: string
      enum: [ "asc", "desc" ]
  sortTitle:
    name: sortTitle
    in: query
    description: Sort todos with title field
    required: false
    schema:
      type: string
      enum: [ "asc", "desc" ]
  sortNotifyAt:
    name: sortNotifyAt
    in: query
    description: Sort todos with notifyAt field
    required: false
    schema:
      type: string
      enum: [ "asc", "desc" ]

definitions:
  404:
    description: Not found
    content:
      application/json:
        schema:
          type: object
          properties:
            message:
              type: string
              example: "Ресурс не найден."
          required:
            - message